.message{data: {'user_id' => message.id}}
  %div{data: {'user_name' => message.user.name}}
  %div{data: {'user_message' => message.text}}
  .upper-message
    .upper-message__user-name
      -# %div{data: {'user_name' => message.user.name}}
      = message.user.name
    .upper-message__date
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  .lower-message
    - if message.text.present?
      -# %div{data: {'user_message' => message.text}}
      %p.lower-message__content
        = message.text
    = image_tag message.image.url, class: 'lower-message__image' if message.image.present?

    //部分テンプレートにどうやってスタイルを当てるのか。indexでレンダーされているのならindexに当ててる部分のscssに記述すればいいのか？